<%= f.label "Pregunta:" %>
<%= f.text_field :text %>
<p>
	<%= f.fields_for :respostas do |resp| %>
		<%= render 'resposta_fields', :f => resp %>
	<% end %>
	<p>
		<%= link_to_add_fields "Afegir resposta", f, :respostas, :resposta %>
	</p>
</p>